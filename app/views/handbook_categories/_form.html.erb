<%= form_for(@handbook_category) do |f| %>
  <% if @handbook_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@handbook_category.errors.count, "error") %> prohibited this handbook_category from being saved:</h2>

      <ul>
      <% @handbook_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
