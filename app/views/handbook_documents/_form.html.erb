<%= form_for(@handbook_document) do |f| %>
  <% if @handbook_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@handbook_document.errors.count, "error") %> prohibited this handbook_document from being saved:</h2>

      <ul>
      <% @handbook_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.text_field :category_id %>
  </div>
  <div class="field">
    <%= f.label :slug %><br>
    <%= f.text_field :slug %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
